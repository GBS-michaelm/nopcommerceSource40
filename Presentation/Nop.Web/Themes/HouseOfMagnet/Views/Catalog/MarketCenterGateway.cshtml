@model CategoryModel

@{
    Layout = "_ColumnsTwo-marketcenter-gateway";

    //title
    Html.AddTitleParts(!String.IsNullOrEmpty(Model.MetaTitle) ? Model.MetaTitle : Model.Name);
    //meta
    Html.AddMetaDescriptionParts(Model.MetaDescription);
    Html.AddMetaKeywordParts(Model.MetaKeywords);
    //page class
    Html.AppendPageCssClassParts("html-category-page");

    var canonicalUrlsEnabled = EngineContext.Current.Resolve<SeoSettings>().CanonicalUrlsEnabled;
    if (canonicalUrlsEnabled)
    {
        var categoryUrl = Url.RouteUrl("Category", new { SeName = Model.SeName }, this.Url.ActionContext.HttpContext.Request.Scheme);
        Html.AddCanonicalUrlParts(categoryUrl);
    }

    var breadcrumbDelimiter = EngineContext.Current.Resolve<CommonSettings>().BreadcrumbDelimiter;


    var themeName = EngineContext.Current.Resolve<IThemeContext>().WorkingThemeName;
    Html.AppendCssFileParts(string.Format("~/Themes/{0}/Content/css/hom-marketcenter-gateway.css", themeName));
    //Html.AppendCssFileParts(string.Format("~/Themes/{0}/Content/css/jquery.fancybox-1.3.4.css", themeName));



    @*<script type="text/javascript" src="@Url.Content("https://ajax.googleapis.com/ajax/libs/jquery/2.2.2/jquery.min.js")"></script>*@
    //Html.AddScriptParts("https://ajax.googleapis.com/ajax/libs/jquery/2.2.2/jquery.min.js");
    Html.AddScriptParts("~/Themes/HouseOfMagnet/Content/js/search-it.js");

}


@using Nop.Core.Domain.Common;
@using Nop.Core.Domain.Seo;
@using Nop.Core.Infrastructure;
@using Nop.Web.Models.Catalog;
@using Nop.Web.Framework.Themes

@{ 
    //might need to pass view model data
    //Model.CustomProperties["maybe VIEW Model"] = "Model data";
    //Model.CustomProperties.Add("maybe VIEW Model",  "Model data");
}



@*<script>
    $(document).ready(function () {
        //$(".fancybox").fancybox({ 'padding': 0 });  //{ 'padding': 0 }
        $('.box-handle').fancybox({ 'padding': 0 });
    });
</script>*@

